<tr class="search-result" style="cursor:default;">
    <td>
        {{#with j}}
        <div class="row jira">{{~space ~}}
            <input class="jira-key" type="hidden" value="{{this}}">{{~space ~}}

            <div class="col-xs-2 jira-id">{{~space ~}}
                <a href="{{jiraLink}}" target="_blank">
                    STU-10000
                </a>{{~space ~}}
            </div>{{~space ~}}
            <div class="col-xs-6 jira-name">Sample Jira Name Sample Jira Name Sampe Jira Name Sample Jira Name</div>{{~space ~}}
            <div class="col-xs-2 jira-status" ><span>Resolved{{status}}</span></div>{{~space ~}}
            <div class="col-xs-2 jira-assignee">Joe Smith{{nameFormat assignee}}</div>{{~space ~}}


        </div>
        {{/with}}
        {{#each a}}
        <input class="asana_id" type="hidden" value="{{this}}">
        {{#asana}}
        <div class="row asana">{{~space ~}}
            <div class="col-xs-2 asana-link">
                <div class="asana-due">{{#if due}}{{formatDate due}}{{/if}}</div><a href="{{asanaLink}}" target="_blank"><span class="glyphicon glyphicon-link"></span></a>
            </div>{{~space ~}}
            <div class="col-xs-8 asana-name">
                {{asanaComplete}} {{name}}
            </div>{{~space ~}}
            <div class="col-xs-2 asana-assignee">{{nameFormat assignee}}</div>{{~space ~}}
        </div>

        {{/asana}}
        {{/each}}
    </td>
</tr>
